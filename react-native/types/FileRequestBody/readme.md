# `react-native-app-helpers/FileRequestBody`

A request body is to contain a file.

## Usage

```tsx
import type { FileRequestBody } from "react-native-app-helpers";

const example: FileRequestBody = {
  type: `file`,
  fileUri: `Example File Uri`,
};
```
