# `react-native-app-helpers/QueryParameters`

A map of query parameter keys to values.

## Usage

```tsx
import type { QueryParameters } from "react-native-app-helpers";

const example: QueryParameters = {
  keyA: `Value A`,
  keyB: 12.34,
  keyC: false,
  keyD: true,
};
```
